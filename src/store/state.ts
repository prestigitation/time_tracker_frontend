import { IState } from '@/types/store'

const state: any = {
    user: <IState> <object> {
        login: undefined,
        id: undefined,
        token: undefined
    },
    locale: "ru"
}

export {state}